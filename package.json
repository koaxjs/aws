{
  "name": "@koax/aws",
  "description": "AWS SDK effect driver.",
  "repository": "git://github.com/koaxjs/aws.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-object": "^1.1.4",
    "@f/thunk-to-promise": "^1.1.1",
    "@yaws/read-region": "^1.0.1",
    "aws-sdk": "^2.2.33"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-polyfill": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "koax": "^0.3.0",
    "tape": "^4.2.2"
  }
}
